# Re-filed findings — wave-1 ultracode fixes reverted, need a careful dedicated pass

**STATUS: ALL 5 FIXED (2026-06-13).** Each re-fixed single-threaded, TDD'd, with a
full-suite green check after — NOT in a parallel batch. Fix order #2 → #5 → #3 →
#4 → #1 (the fork hazard first, since #5 depended on it being gone).

| # | Finding | Commit |
|---|---------|--------|
| 2 | run_shell preexec_fn fork-from-multithreaded (the hang root) | `593a8df` |
| 5 | team_memory LanceDB rebuild/read race | `5bce6a2` |
| 3 | lint path args not confined to artifacts root | `9a6e54c` |
| 4 | unsandboxed-fallback drain unbounded | `9a6e54c` |
| 1 | redo/resume dependency ordering (serial, no wave threads) | `8f468b8` |

Verification: full suite green (3297) after #1; the previously-hanging
`test_app_launches_in_stub_mode` passes under randomized order alongside the
tools/security tests; each fix carries a TDD regression proven to fail without
the fix. Pending: Nemo hull review of the 5 (concurrency/correctness surface).

These are REAL findings from the full-debug review. The wave-1 parallel-agent fix
for each was either buggy or introduced a subtle full-suite hang, so it was
reverted (a botched fix is worse than the original bug).

## 1. orchestration.py — redo/resume dependency ordering (HIGH, opus #2)
`_reexecute_goal` / `_auto_resume_refreshable_goals` re-run reopened tasks in
store-list order with no dependency gating or dep-failure cascade. The wave-1 fix
added a `_run_reopened_tasks` helper (topological sort + dep-gate) — correct in
spirit, but it routed re-execution through a path that opened the `#80` fix-window
and, combined with the LanceDB/fork issues below, contributed to a suite hang.
Reverted to the original serial redo. **Re-fix:** add dependency ordering to the
two re-run lanes WITHOUT changing the fix-window/redo path semantics; verify no
fix-window thread leaks under the test callbacks. (The dead `_run_reopened_tasks`
helper + its 2 unit tests in `test_orchestration_stability_fixes.py` remain — fold
them into the proper fix or remove.)

## 2. tools.py — run_shell preexec_fn fork-from-multithreaded (HIGH/race, opus)
`run_shell` uses `Popen(preexec_fn=_apply_child_rlimits)` — forking a multithreaded
process and running Python in the child between fork and exec is a deadlock hazard
(it can wedge thread-start in the PARENT too, which is what hung the TUI test in the
full suite). The wave-1 fix hoisted `import resource` but kept the Python
`preexec_fn`, so the fork-from-multithreaded remained. **Re-fix:** drop the Python
`preexec_fn` entirely — apply RLIMIT_AS/FSIZE/CORE via `resource.setrlimit` is
fork-unsafe; instead use `posix_spawn`-style or set limits via a tiny exec wrapper,
OR accept `start_new_session=True` + the wall-timeout/process-group-kill as the
containment (drop the rlimits) since bwrap already bounds the sandboxed case.

## 3. tools.py — mypy/pyflakes/ruff file-path args not confined to artifacts root (HIGH/correctness, opus)
The passive-profile lint branches don't run `_is_safe_file_arg` on the file args, so
a lint invocation can read an arbitrary source file. Reverted with #2. **Re-fix:**
add `_is_safe_file_arg(a, root)` to the mypy/pyflakes/ruff arg validation
(standalone — this one is safe + simple, just got reverted alongside #2).

## 4. tools.py — unsandboxed-fallback drain unbounded (MEDIUM/resource-leak, opus)
After the timeout killpg, `proc.communicate()` has no timeout — a double-forking
child hangs run_shell forever. **Re-fix:** bounded `communicate(timeout=...)` +
second kill (the wave-1 fix was correct; just reverted alongside #2).

## 5. memory/team_memory.py — _ensure_vectors LanceDB rebuild race (HIGH/race, opus H10)
Concurrent wave workers can race on `db.drop_table` / `create_table` of the shared
LanceDB table. The wave-1 fix added `_VECTOR_BUILD_LOCK`; reverted because it (or
LanceDB's background runtime) participated in the suite hang. **Re-fix:** serialize
the destructive rebuild with a lock, but verify it doesn't leave a LanceDB
background thread that interacts with the run_shell fork (item #2) — fix #2 first.

---

The other ~44 wave-1 fixes landed on `fix/0.9.0-stability`. These 5 + the 49 LOW
findings are the remaining 0.9.0 stability work.

## LOW wave — DONE (2026-06-13, commit `14de83f`)
49 LOW findings swept via ultracode (one agent/file): **39 fixed** (real latent
bugs, minimal agnostic fixes + TDD regressions), **8 dropped** (1 already-fixed,
3 false-positive, 4 not-worth-churn — all with rationale). 3398 green, ruff clean.

### Cross-file follow-ups (surfaced by the LOW wave)
- **#64 — cron.add JT fit-gate — DONE (commit `2fb8967`).** `cron.add` now uses
  the strict `unfilled_required` (matches the run-time `_jt_fit` gate), so a
  present-but-empty required param is refused at add time instead of every
  headless cycle.
- **#85 — TUI cron-filter — DONE (commit `2fb8967`).** Added
  `CronScreen.focus_project` (the sibling the app.py half called); `/cron <code>`
  now applies the project filter end-to-end.
- **#73 — plan-time media-deliverable signal — DEFERRED (design, not cleanup).**
  `_normalize_render_paths` rewrites `X.docx/pdf/pptx/odt/rtf/epub → X.md` in goal
  TEXT at decompose time so the contract names the authored Markdown source, not
  the rendered artifact. The concern: a MEDIA-assembled deliverable in one of
  those container formats would be wrongly forced into the document/prose lane.
  **There is no safe minimal fix:** at decompose time the structured
  `artifact_kind` doesn't exist yet (decompose is producing the structure from
  the text), and narrowing the regex is unsafe — pandoc legitimately renders
  Markdown to *all* of docx/pdf/pptx/odt/rtf/epub, so dropping any of them breaks
  the primary document-render path (e.g. the HRWT PDF anthology). A correct fix
  carries/derives an artifact-kind/family signal into the decompose normalization
  — real design touching the decompose pipeline, and it belongs with the
  deliverable-fidelity / media-awareness arc, not this stability pass. Filed as a
  design task; the document path is correct today and the media-container case is
  an untriggered edge.

## R2 full-debug fix campaign — DONE (2026-06-13/14)
After v0.8.9, a full-codebase RE-debug on `fix/0.9.0-stability`: Opus ultracode pass
(111 findings: 6 HIGH / 51 MED / 54 LOW) + 3 cadre passes (Nemo GPT-5.5, Nemo MiniMax,
Lovecraft-on-Grok), with both cadre models cross-verifying the 6 Opus HIGH (0 refuted).

- **6 HIGH clusters — BY HAND, TDD, gated:** A store-read resilience (binary/non-UTF-8/
  BOM/CRLF/non-dict + atomic write) `5388c4c`; C qc_history LanceDB lock `0bc927d`; B
  zero-completed goal-settle (shared helper, 3 lanes) `688442e`; D wave worker-state
  (budget ContextVar per-future copy_context + binary twin recording + write_artifact
  merge callback) `0faf81e`.
- **3 security/secret-race MEDs — BY HAND:** standards_proposals frontmatter-injection +
  path-traversal + ruff --fix in passive tier `1ee8bd6`; config write_secret_file unique
  temp `8e885a9`.
- **87 MED+LOW — ultracode wave** `91c500f` (88 fixed −1 reverted; 15 dropped; 14 cross-file).
  REVERTED: the dispatch_breaker anti-false-positive change (gated repetition-trip on
  no-commit) weakened the loop-breaker + broke 2 intended-behavior tests — see below.

3682 green + ruff clean in fixed AND randomized order.

### Re-filed (deferred, NOT done)
- **dispatch_breaker repetition false-positive (MED, product-agnostic).** A legit
  data/markdown table or repeated code/config with ≥ _REPEAT_THRESHOLD identical windows
  trips the repetition breaker and discards a committed-good file. The wave's fix (only
  trip repetition when ~nothing committed) is UNSAFE — "committed" is an unreliable signal
  (storm text gets written to the draft too), so it lets a committed-garbage storm through
  and broke `test_breaker_trips_in_diff_mode`/`_in_llm_with_tools`. Reverted. Needs a
  targeted fix that distinguishes structured repetition from degenerate prose loops without
  loosening the no-progress guard.

### Cross-file follow-ups from the R2 MED+LOW wave — RESOLVED 2026-06-14 (commit below)
- **DONE (by hand):** `skill_library._read_frontmatter` errors='replace' (non-UTF-8 skill
  no longer crashes the index build); `repo_map.build_repo_map` now counts the name-only
  "listed only" lines + hard-stops with a "+N more" summary (the cap-bypass); `model_presets`
  factored `_reject_secret_auth_config` + applied it in `update_preset` too (the
  `configuration.register()` add→update fallback no longer bypasses the secret keel);
  `cli.py` TUI-launch now calls `_relaunch_if_restart` (honors `/restart` exit-42).
- **ALREADY FIXED by their own wave agent (redundant notes):** `standards._parse_file`
  read guard; `roster` `capacity_cap` floored at 1 via a field_validator.
- **DEFERRED — ACP project-scoped converse lock.** Re-read of the code: `_append_conversation`
  already writes with atomic `os.open(O_APPEND)` at 0600, so concurrent appends **cannot
  corrupt or lose data** — each line lands atomically. The residual is only *logical
  interleaving* of a deliberately-SHARED project conversation across sessions (low impact),
  NOT the "read-modify-write corruption" the finding implied. A correct fix (narrow
  project-keyed lock around just the load→append-operator window — never the model call /
  permission wait, which deadlocks, proven) is subtle and belongs with the §2 multi-session
  ACP / permissions work, not a rushed stability patch. Not a ship blocker.
- **DEFERRED — semantic_router import-time `_EMBED_MODEL` (LOW).** team_memory/qc_history now
  resolve the embed model per-call; semantic_router still binds it at import (a wizard
  model-swap mid-process won't rebuild). Foundational module (the embedder) — blast radius
  too high to change late; a LOW. Re-file for a careful pass.
- Sibling-mirror error-path/encoding guards: `standards.py` + `skill_library._read_frontmatter`
  bare `read_text()` (same UnicodeDecodeError class the wave fixed in skills/qc_history/etc.);
  `repo_map.build_repo_map` MAX_MAP_CHARS bypass (same as team_canvas); `semantic_router`
  captures embed-model at import (same as team_memory); `configuration.update_preset` must
  run the same auth_config secret-leak keel as `add_preset`; `cli.py` TUI-launch path also
  swallows the `/restart` exit-code 42; shared `safe_status()` TUI markup helper.

## Pre-ship RE-SWEEP campaign — DONE (2026-06-14)
After the merge to local main, two more full-codebase Opus ultracode sweeps (the
"keep going until the review is complete end-to-end" mandate), chunked-4 to dodge
server-side overload. First gave 76 findings (6 raw HIGH → adversarial-verify
re-rated 5 to MEDIUM, leaving 1 true HIGH; 31 MED / 39 LOW). All resolved on
`fix/0.9.0-preship`:

### By hand (HIGH + systemic clusters) — `ce49a25`, `5a7f258`
- **HIGH — assembly PDF regression** (`5a7f258`): render_document's pdf branch had
  lost its `pandoc md→docx` step in the pre-ship resource-leak refactor → soffice
  got an EMPTY docx → contentless PDF (the HRWT fabricated-binary class). Restored
  + a regression that asserts the pandoc→soffice sequence on a non-empty docx (the
  leak tests mocked pandoc but never asserted it ran).
- **Encoding-READ cluster** (`5a7f258`): 57 bare `read_text()` → `encoding="utf-8"`
  (the C/POSIX-locale-can't-read-utf-8 bug). JSON/cache/index/credential readers
  also `errors="replace"` (garbage → invalid JSON → existing except → empty/rebuild)
  + 3 `_load_meta` readers broadened to OSError. CAREFUL-REPAIR: reverted
  `errors="replace"` on 4 TEXT readers with deliberate detect-and-skip / fail-open
  contracts (recoveries, backup._walk_vault, team_memory._parse) — encoding-only
  preserves their existing UnicodeDecodeError handling.
- **#1437 FAILED cross-goal dependency** (`5a7f258`): the flip side of the cross-goal
  topo fix — `_ready_wave`/`_dep_failed` treated any absent dep as satisfied, so a
  later goal ran against a prior-goal input that terminal-FAILED. Added
  `_cross_goal_dep_status` (store lookup); both the wave executor and the resume gate
  now block on a FAILED cross-goal dep and wait on a not-yet-COMPLETED one.
- **3 systemic clusters** (`ce49a25`): the encoding-WRITE sweep, the TUI markup-escape
  surfaces, and the cross-goal topo store-validation (a typo'd dep still rejects;
  only a real cross-goal id is filtered) all landed here first.

### Fix-wave (43 MED/LOW across 33 files) — `079f59a`
One agent per source file. Token-native regress guard; guarded WIN_CODIFY_FLOOR
int(); inbox priority validation; heartbeat cancel-aware requeue + full-token dep
match; budget record lock; cron cross-process lock; O-series regex; sandbox env-deny;
metered recursion bound; oauth single-flight; telegram caption CRLF; roster cap on
model_copy; daemon child guard; TUI unmount/markup guards; wizard BACK/key + step-order.

### REJECTED (investigated false-positives)
- **#11927 initial-pass zero-completed goal "strands" IN_PROGRESS** — NOT a bug: two
  reviewer-tested contracts (capability-blocked, all-QC-rejected) deliberately keep the
  goal IN_PROGRESS awaiting human ticket resolution + next-kickoff auto-resume. Settling
  it COMPLETED would falsely mark genuinely-pending goals done. The fresh verifier saw
  the gate in isolation. (My first fix attempt broke both tests → reverted.)
- converse()→_pin_attachments (read-only chat turn, by design); emit_compaction lock
  (already runs under `_claim_plan_lock`).

### DEFERRED (real, scoped out — non-blocking)
- **_seed_staging full-tree copytree per task/wave** (perf): hardlink seeding is UNSAFE
  (producers commit via `write_text`, which truncates the inode → cross-task bleed). A
  correct copy-on-write/selective-seed fix is cross-cutting; post-0.9.0.
- **Resume topo path drops unknown-ref deps** (LOW, #7a asymmetry): the correct fix
  mirrors the main path's store-validation, but it collides with an existing cross-agent
  contract test; scope separately.
- Plus the prior-round defers (dispatch_breaker repetition false-trip; semantic_router
  import-time embed model; ACP project-scoped converse lock).

### Process incident (caught, not shipped)
The fix-wave's FIRST run reported "41 fixed / 0 failing" but a merged-tree check found
~20 source edits silently dropped: concurrent agents used `git stash` for fail-first TDD
and collided on the global stash (untracked test files survived; tracked source edits
were lost). Re-ran with a hard NO-GIT rule + idempotent self-heal (read-first,
apply-if-missing). Verified the MERGED tree (33 source files modified, fix markers
present), not the agent reports. Lesson recorded in auto-memory.

3rd full re-sweep running to confirm convergence (trend 111 → 99 → 84 → 76). 4039 green
fixed + randomized, ruff clean, on `fix/0.9.0-preship`, LOCAL.

## 3rd re-sweep round — DONE (2026-06-14)
3rd full sweep: 47 findings, **0 true HIGH** (2 raw HIGH adversarially re-rated MEDIUM:
plans symlink re-entrant lock — gated behind a non-default vault config; runners tool-loop
OAuth-refresh drift — OAuth-preset-specific). Trend 111 → 99 → 84 → 76 → 47.

### By hand — `2f8a8b4`
- **#11951 — cross-goal cascade, 3rd path**: the SEQUENTIAL-fallback execution path
  (MODULATIO_CONCURRENT_WAVES=0) computed failed_deps inline against in-goal task_map only.
  Routed through the shared `_dep_failed(t, task_map, cross_goal_status)` gate — now all
  THREE paths (concurrent wave / sequential fallback / resume) enforce the FAILED-cross-goal
  block uniformly.
- **Encoding-WRITE stragglers**: the write_text/read_text sweep missed the `.open("a")` /
  `os.fdopen` append + atomic-temp-write side. 13 sites pinned utf-8 (recoveries, comptroller,
  budget, lessons, project_execution, orchestration transcript, config/team_state/
  context_budget/_crash/tool_summarization atomic writes, daemon log, plans lock) + delivery
  prose read. The encoding class is now closed on BOTH directions.

### Fix-wave (39 across 30 files) — `13ade15`
Both former-HIGHs (plans symlink-lock key → resolve() unconditionally; runners tool-loop
OAuth refresh+alert) + orchestration (deny-ticket affected_goal_id, similar_verdicts
defensive wrap, path-only attachment inline, stale-ticket retire, token-native regress
audit, wave-reflect assembler-skill guard); cron/heartbeat/auth_alerts cross-process write
locks; skills._SKILLS_ROOT per-call; store quarantine model; TUI markup/dup-id/double-clear;
provider_catalog/semantic_router/review_ledger/dispatch/telegram/wizard.

### Skipped/Deferred
- Skipped-FP: ACP server converse-lock (correct fix is in orchestration.converse, not server).
- Deferred: _open_budget_exhausted_ticket dead-code removal (risky); _seed_staging copytree→
  hardlink (UNSAFE — write_text truncates the inode). Plus prior defers (dispatch_breaker
  repetition unit-blindness; ACP project-scoped converse lock).
- Cross-file follow-ups re-filed (LOW, non-blocking): comptroller idempotent_reuse → runners
  re-spend skip (metered tier, no real provider wired); attachments build_attachment central
  regular-file guard; agent_wizard markup escape; repo_map rglob symlink guard.

### Process note
The fix-wave's first run dropped ~20 source edits to concurrent `git stash` collisions; re-ran
with a hard NO-GIT rule + idempotent self-heal + fresh per-round test files (_resweep_r3.py),
verifying the MERGED tree each time. 4178 green fixed + randomized, ruff clean.

4th/final confirming sweep running.

## 4th/final confirming sweep + close-out — DONE (2026-06-14)
4th sweep: 23 findings, **0 true HIGH** (3rd consecutive clean sweep; 2 raw HIGH re-rated
MEDIUM: tool-loop assembly-output-file leak; TUI oversized-attachment crash). No systemic
cluster recurrence (cross-goal 0, encoding down to 3 reader stragglers). Trend
111 → 99 → 84 → 76 → 47 → 23.

### Close-out wave (23/23 fixed, 0 skipped, 0 deferred) — `daf3582`
orchestration (tool-loop honors AssemblyRecord.output_file; dynamic backtick fence;
QC-fixer escalation defect_type; wave-merge sidecar-vs-sibling-primary guard); backup
(project-relative cache exclusion — fixed a SILENT TOTAL backup drop when the vault lives
under a .cache ancestor; import non-str content → clean ValueError); TUI oversized-attachment
catch at 3 sites + agent_wizard escape; metered idempotent_reuse honored; cron JT-default
fit-gate; cli_standards multi-word kind; standards_proposals/inboxes _load_jsonl resilience;
agent_memory/research atomic writes; provider_step picklist guard + align;
project_execution tz-aware; runners tokenizer; standards traversal guard.

### GATE STATUS — MET
- 0 true HIGH across 3 consecutive sweeps (the last HIGH, an assembly PDF regression, fixed).
- Systemic clusters CLOSED: encoding (read + write/append), cross-goal FAILED-dep (all 3
  execution paths), TUI markup, cross-process write locks.
- Diminishing: true-HIGH 6→1→0; findings 111→76→47→23, halving each pass — the asymptotic
  scattered-LOW tail of a 58-KLOC codebase.
- 4257 green, fixed + randomized; ruff clean.

### Honest caveats (non-blocking, documented)
- The close-out wave's own 23 fixes were NOT themselves re-swept (localized error-path/escape/
  atomic-write changes; suite-verified both orders). A 5th sweep would be the tail of the tail.
- One non-deterministic PytestUnhandledThreadExceptionWarning seen once in teardown; did not
  recur under -W error or on re-run.
- Standing deferred (post-0.9.0): dispatch_breaker repetition unit-blindness; _seed_staging
  copytree→COW; ACP project-scoped converse lock; _open_budget_exhausted dead-code removal.

ALL on `fix/0.9.0-preship`, LOCAL. Ready to merge → 0.9.0 (Clif's call + public-push auth).
