# 0.9.0 PRE-SHIP sweep findings (merged main) — 2026-06-14

Final full-codebase sweep on the MERGED 0.9.0 main (the ship artifact). 22/22 subsystems (run1 orch-wave-assembly + chunked backfill of 21). **0 HIGH (4 raw-HIGH all verify-revised to MEDIUM) / 43 MED / 56 LOW.** Loop-until-dry: fix all, re-sweep, repeat until clean.

## MEDIUM
- **[MEDIUM/correctness]** `src/modulatio/delivery.py:271` (assembly-delivery) — A genuinely-rendered .rtf deliverable is re-rendered (corrupted) because RTF is text, not binary
  - `_is_prose_source` returns True for any `.pdf/.docx/.doc/.odt/.rtf` whose bytes are not `_looks_binary`. `_looks_binary` flags binary only on a NUL byte or non-UTF-8 decode. RTF is a pure-ASCII, text-based format (`{\rtf1\ansi...}`) with no NUL and valid UTF-8, so `_looks_binary` returns False for a REAL, correctly-rendered RTF document. Both the assembler (`render_document` / `_PANDOC_DIRECT_FORM
  - FIX: Don't rely solely on `_looks_binary` for the prose-vs-verbatim decision on declared-binary extensions. For an extension that is a DECLARED rendered format (`.rtf`, and any future text-based binary like `.tex`), gate on the format signature (reuse `review_ledger.verify_declared_format` / a `{\rtf` sn
- **[MEDIUM/resource-leak]** `src/modulatio/assembly.py:1233` (assembly-delivery) — PDF render path orphans a partial `<stem>.pdf` in artifacts_root when libreoffice fails
  - In `render_document`'s `fmt == "pdf"` branch, `_run_doc_tool(["soffice", ...])` is called with `--outdir artifacts_root`. If soffice exits non-zero, `_run_doc_tool` raises `_DocToolError` BEFORE the `pdf_out = docx_tmp.with_suffix('.pdf')` / `pdf_out.is_file()` lines. The `finally` at line 1247 only unlinks `docx_tmp` (the .docx temp), never `pdf_out`. If libreoffice wrote a partial/garbage `<stem
  - FIX: Compute `pdf_out` before the soffice call (or in the `finally`) and unlink it on any failure, mirroring the pandoc-direct branch: wrap the soffice call so that on `_DocToolError`/`_MediaToolError` `pdf_out.unlink(missing_ok=True)` runs, and ensure the `finally` also clears a leftover `pdf_out` on th
- **[MEDIUM/security]** `src/modulatio/_crash.py:102` (cli) — Crash-log traceback body is written UNREDACTED — secret-scrubbing only covers argv
  - write_crash_log redacts argv via _redact_argv, but the traceback string `tb` (line 102) is written verbatim into the crash file. LLM-client exceptions (litellm/httpx auth errors) routinely embed the request URL or auth header in str(exc); a base_url configured with `?api_key=sk-...` or an auth error echoing a bearer token lands in the log unredacted. The module already has `_scrub_embedded_secrets
  - FIX: Apply `_scrub_embedded_secrets(tb)` to the traceback before writing, and extend `_EMBEDDED_SECRET` to also match `Bearer <token>` / `Authorization: Bearer <token>` (space-separated) forms. Belt-and-suspenders the same way argv is handled.
- **[MEDIUM/product-agnostic]** `src/modulatio/cli.py:441` (cli) — `kickoff --attach` hardcodes kind='document' — cannot improve a binary/image/media artifact, opaque utf-8 error
  - Every --attach path is built with `build_attachment(_path, kind="document")`, which calls `path.read_text(encoding="utf-8")`. For any non-text artifact class (image PNG/JPG, PDF, zip, compiled binary, media) this raises UnicodeDecodeError, caught at line 445 and surfaced as `! --attach: cannot attach <path>: 'utf-8' codec can't decode byte 0x89 in position 0`. attachments.AttachmentKind supports '
  - FIX: Infer the attachment kind from the path (extension/magic byte) or add an explicit `--attach-kind` option, routing images to kind='image'; at minimum catch UnicodeDecodeError specifically and emit an artifact-class-aware message (e.g. 'binary/image attachments not yet supported via CLI; only text/doc
- **[MEDIUM/integration]** `src/modulatio/context_budget.py:121` (context-budget) — `--ctx-budget researcher=N` validates but is a silent no-op — alias mismatch with dispatch's `budget_role="research"`
  - `parse_cli_override_specs` accepts `researcher` as a valid role (it's in `EXPERIMENTAL_DEFAULTS` at line 121, kept as a documented back-compat alias per the comment at lines 118-119). The CLI (cli.py:148) registers the override keyed by the raw role string: `overrides["researcher"] = BudgetOverride(...)`, and even prompts the operator for confirmation/reason and prints `ctx-budget override: resear
  - FIX: Either (a) collapse the alias at parse/registry time: when role=='researcher', store under key 'research' (and warn the alias is normalized); or (b) make `_user_override_for` alias-aware (map 'researcher'->'research' on lookup); or (c) drop 'researcher' from `valid_roles` in `parse_cli_override_spec
- **[MEDIUM/cost]** `src/modulatio/comptroller.py:416` (cost-metering) — Metered per-task cap conflates distinct cost classes (latent — bites first real adapter)
  - In _scan_metered_today, task_count increments for ANY metered ledger line with entry_task == task_id, regardless of cost_class (line 416 has no `entry_cost == cost_class` guard, unlike the cost_count branch at 414). So if one task issues a paid-cloud metered call and then a premium-cloud metered call (two different metered tools, two different budgets), the second is spuriously DENIED by the per-t
  - FIX: Scope the per-task count to the cost_class being authorized (and arguably to the tool): change line 416 to `if entry_cost == cost_class and entry_task == task_id:` (or thread tool_name into the metered ledger line and count per (task, tool)). Mirror the existing cost_count guard.
- **[MEDIUM/error-path]** `src/modulatio/constitution.py:58` (cross-cutting) — Non-UTF-8 constitution file crashes Leader prompt-build (except OSError misses UnicodeDecodeError)
  - load_constitution() reads each candidate with path.read_text(encoding="utf-8") inside a try/except that catches ONLY OSError. A non-UTF-8 byte in the project/shared/seed constitution file raises UnicodeDecodeError, which is a ValueError subclass — NOT an OSError — so it propagates out of the loader. The module docstring and the loader contract promise it is injected only into the conversational Le
  - FIX: Change the except clause to `except (OSError, ValueError):` (or `except (OSError, UnicodeDecodeError):`) so a non-UTF-8 file degrades to skip, matching the documented defensive contract and qc_notes.py.
- **[MEDIUM/error-path]** `src/modulatio/qc_persona.py:66` (cross-cutting) — Non-UTF-8 qc_persona file crashes QC review prompt-build (except OSError misses UnicodeDecodeError)
  - load_qc_persona() reads candidates with read_text(encoding="utf-8") under `except OSError`. A non-UTF-8 persona file raises UnicodeDecodeError (ValueError subclass, not OSError) which propagates into the QC review prompt-build (orchestration.py:6169). The docstring says it is injected only into the QC review prompt and that the seed/shared/project files are user-editable — a user pasting a non-UTF
  - FIX: Catch (OSError, ValueError) (or add UnicodeDecodeError) on the read inside load_qc_persona, mirroring qc_notes.load_standing_notes.
- **[MEDIUM/error-path]** `src/modulatio/design_intent.py:68` (cross-cutting) — Non-UTF-8 design-intent file crashes producer/QC/Leader prompt-build (except OSError misses UnicodeDecodeError)
  - design_intent.load_body() reads the file with path.read_text() under `except OSError`. A non-UTF-8 design-intent.md raises UnicodeDecodeError (not OSError), propagating into render_for_prompt and its four call sites (chat.py:145, orchestration.py:3221/3892/5493 — producer, decompose, QC, Leader). The docstring explicitly states: 'a broken design-intent file should NOT block producer dispatch.' A b
  - FIX: Catch (OSError, ValueError) on the read; optionally pass encoding="utf-8" for determinism. Matches qc_notes.py's robust handling.
- **[MEDIUM/error-path]** `src/modulatio/ab_harness.py:1257` (cross-cutting) — Non-UTF-8 audit/usage jsonl fails the whole replicate instead of per-line skip
  - _read_audit_rows (1257) and _read_usage_rows (1277) call read_text(encoding="utf-8").splitlines() and only catch json.JSONDecodeError per-line. The docstrings claim robustness to 'partial corruption' with malformed lines skipped. But a single non-UTF-8 byte (e.g. a crash that truncated a multibyte char mid-write to audit.jsonl) raises UnicodeDecodeError BEFORE any line is parsed. extract_metric_sn
  - FIX: Read with errors='replace' or wrap the read_text in try/except (OSError, UnicodeDecodeError) returning [] (or read line-by-line in binary and decode per line) so corruption degrades to skipped lines, not a lost replicate.
- **[MEDIUM/integration]** `src/modulatio/chat.py:81` (interfaces) — Multimodal (image-attachment) chat drops project design-intent + skill injection
  - chat_with_agent routes any message containing an image attachment to chat_with_agent_multimodal, but does NOT pass project_code, and chat_with_agent_multimodal (src/modulatio/multimodal.py:165) accepts no project_code and does not inject the design-intent block or agent skill bodies. The single-prompt path (_build_prompt) injects design-intent into 'every chat prompt' and the agent's skills; the m
  - FIX: Thread project_code through chat_with_agent_multimodal and build_multimodal_messages, rendering design_intent.render_for_prompt(project_code) + _load_agent_skill_bodies into the system/first-user message, mirroring _build_prompt. Or hoist the identity+intent+skill prefix into a shared builder both p
- **[MEDIUM/resource-leak]** `src/modulatio/acp/server.py:163` (interfaces) — PendingRequests slot leaks when write_message raises before wait()
  - request_and_wait calls self._pending.register(rid) then enters try: rpc.write_message(...) then self._pending.wait(rid). The _pending slot is popped ONLY inside wait(). If write_message raises (broken pipe / closed stdout to the ACP client), wait() never runs, so _pending._slots[rid] is never popped. The finally block cleans only _session_pending, not _pending. Every failed server-initiated reques
  - FIX: In request_and_wait, also discard the _pending slot in finally (e.g. self._pending.wait(rid, 0) to pop, or add a _pending.discard(rid) helper) so a write failure doesn't strand the slot.
- **[MEDIUM/correctness]** `src/modulatio/semantic_router.py:90` (memory) — FastEmbedder.dim hardcoded 384 but embedding_model is wizard-overridable to a non-384-dim model
  - FastEmbedder.dim = _EMBED_DIM = 384 is a constant, but the actual model is config.get_embedding_model() which the wizard can override to any HuggingFace model. team_memory._ensure_vectors and qc_history._ensure_verdict_vectors build the pyarrow schema with pa.list_(pa.float32(), embedder.dim) (=384) while embedder.embed_texts returns vectors of the REAL model's dimension. If a user sets embedding_
  - FIX: Derive FastEmbedder.dim from the model (query fastembed for the model's dimension at load, or maintain a model->dim map keyed on get_embedding_model()), so embedder.dim always matches the vectors it emits. semantic_router also computes _EMBED_DIM=384 statically and uses it in ensure_agent_vectors sc
- **[MEDIUM/race]** `src/modulatio/memory/agent_memory.py:179` (memory) — get_episodic/add_episodic do unlocked read-modify-write of the same JSON file (lost updates)
  - get_episodic loads the whole episodic.json, then rewrites the entire file to bump access_count/last_accessed (line 194). add_episodic also does load-append-prune-save. Neither holds a lock. team_memory and qc_history were given explicit per-project locks for exactly this kind of shared-state hazard, but agent_memory was not. If two operations touch the same agent_id+project concurrently (e.g. a ch
  - FIX: Guard the per-(agent,project) episodic/semantic file read-modify-write with a per-path lock (mirror team_memory's _vector_lock keyed by the file path), or write atomically with a compare/merge. At minimum, avoid rewriting the whole file from get_episodic on a read path, or do it under a lock.
- **[MEDIUM/race]** `src/modulatio/semantic_router.py:95` (memory) — Shared FastEmbedder is lock-free: double model-load race + concurrent ONNX inference across wave workers
  - A single FastEmbedder instance is constructed once (cli.py:484 / daemon.py:361) and passed to all three vector subsystems: the routing matcher, qc_history_embedder, and team_memory_embedder. Concurrent wave workers run _execute_task_isolated in a ThreadPoolExecutor (orchestration.py:7224). Each worker calls _recall_team_memory (team_memory.recall -> embedder.embed_text) and QC (qc_history.similar_
  - FIX: Make FastEmbedder thread-safe: guard _get() with a threading.Lock for the lazy load (double-checked), and serialize embed_text/embed_texts inference with an instance lock (or document+enforce that the shared embedder must only be touched under a single global embed lock). The per-project / per-(proj
- **[MEDIUM/integration]** `src/modulatio/orchestration.py:5326` (orch-converse) — converse() offline guard checks chat_runner, but the vision turn dispatches via agent_models/leader_model — the two can disagree
  - converse()'s offline short-circuit tests `_resolve_chat_runner("leader") is None`. But the vision branch (`elif has_image`) routes through _run_multimodal_leader, which does NOT use chat_runner at all — it resolves the model from `project.agent_models['leader'] or project.leader_model` and calls litellm.completion directly (lines 2454-2467). So the offline guard does not reflect whether the multim
  - FIX: For the has_image branch, base the offline decision on the multimodal model resolution (agent_models['leader'] or leader_model) rather than _resolve_chat_runner('leader'); e.g. compute leader_model_id once and branch offline when neither the chat runner (text path) nor the multimodal model (vision p
- **[MEDIUM/correctness]** `src/modulatio/orchestration.py:404` (orch-exec) — pptx in evidence-normalization regex but absent from renderable doc formats → document-family pptx deliverable cannot succeed (P5 reject loop)
  - `_RENDER_DELIVERABLE_RE` (line 404-406) matches `docx|pdf|pptx|odt|rtf|epub`, so for the DOCUMENT family `_normalize_render_paths` rewrites `deck.pptx -> deck.md` for evidence target/source. But the deliverable's `output_path` is NOT normalized (correct, by #73 design) and stays `deck.pptx`. `_DOC_RENDER_EXTS` (line 5747-5749) is `{docx,odt,rtf,epub,pdf}` — it OMITS pptx — and pandoc's `_PANDOC_DO
  - FIX: Remove `pptx` from `_RENDER_DELIVERABLE_RE` so a `.pptx` deliverable keeps its real extension (routed/treated like a media composite, e.g. a slideshow built directly), matching `_DOC_RENDER_EXTS` and pandoc's actual capability. Equivalently: only normalize extensions the engine can actually render b
- **[MEDIUM/product-agnostic]** `src/modulatio/orchestration.py:9392` (orch-goal) — Redo loop-breaker fingerprint is BLIND to binary/media deliverables — forces false 'stalled' after one redo
  - _goal_deliverable_fingerprint hashes self._read_task_artifact(t) or "", and _read_task_artifact returns None for any non-utf-8 file (binary/media deliverable raises UnicodeDecodeError → None, lines 9334-9339). So every binary/media deliverable hashes to the same empty body "" on every round regardless of its actual content. The loop-breaker (lines 8697-8707) then sees the fingerprint as UNCHANGED 
  - FIX: Fingerprint the actual file BYTES for non-text deliverables. In _goal_deliverable_fingerprint, resolve the path via _task_artifact_path and hash path.read_bytes() (size+content) when text decode fails, instead of collapsing to "". Keep the text path for utf-8 artifacts. This makes the loop-breaker m
- **[MEDIUM/correctness]** `src/modulatio/orchestration.py:8651` (orch-goal) — Leader recommendations duplicated in the PQR across every redo round (no dedup)
  - _record_recommendations(goal, data.get('recommendations'), summary) runs UNCONDITIONALLY on every _leader_verify_goal invocation, before the verdict branch. The §3b redo path re-enters _leader_verify_goal recursively (line 8751→8288), so for a goal that redoes N-1 times the verifier runs N times and appends that round's model reservations to the shared summary.recommendations each time. summary.re
  - FIX: Dedup summary.recommendations before rendering (e.g. in _deliver_finished_products before deliver_product_quality_report, drop exact (goal_id,concern,suggestion) duplicates), or only record the model's recommendations on the TERMINAL verify (don't re-record on a verify that is about to redo).
- **[MEDIUM/product-agnostic]** `src/modulatio/orchestration.py:3403` (orch-wave-assembly) — #73 render-path normalization mis-routes media UNIT producers whose artifact_kind isn't a shipped standards domain
  - _effective_assembly_family() routes evidence normalization off standards.load_with_metadata(artifact_kind), where artifact_kind is the planner's FREE-FORM string used directly as a standards filename (no canonicalization — confirmed: no alias/synonym map exists). Only the exact shipped domains image/audio/video/data/code declare a non-document assembler_skill. A media UNIT producer task (required_
  - FIX: Canonicalize artifact_kind to a known assembly family before the standards lookup (a small kind->family/synonym map, e.g. slide/deck/presentation/spread->image-or-media), OR make the render-extension rewrite gate on the EVIDENCE's own extension class rather than the artifact_kind-derived family: nev
- **[MEDIUM/edge-case]** `src/modulatio/plans.py:378` (planning) — read_text() lacks encoding="utf-8"; write path is UTF-8 — UnicodeDecodeError on non-UTF-8 locale (daemon/cron), bricks plan load + whole project listing
  - persist() writes plan files with open(target,"x",encoding="utf-8") (line 315) and yaml allow_unicode=True, so bodies/frontmatter legitimately contain non-ASCII (CJK titles, em-dashes, emoji — expected for a product-agnostic engine). But every READ path — load() L378, set_status() L597, update_execution_state() L794 — calls target.read_text() with NO encoding, defaulting to the process locale. Unde
  - FIX: Add encoding="utf-8" to all three read_text() calls (L378, L597, L794) to mirror the UTF-8 write path. Same fix applies to team_state.load_body L138 (its except OSError does NOT catch UnicodeDecodeError, which is a ValueError subclass).
- **[MEDIUM/correctness]** `src/modulatio/plans.py:499` (planning) — extract_sub_objectives silently DROPS a malformed sub-objective: cross-check detects the count mismatch but only logs a warning — engine runs a short list and reports the plan done with a user-approved objective never executed
  - _SUB_OBJECTIVE_ITEM_RE requires a closing ** on the title line. A model glitch (missing closing bold, e.g. '**2. Second task that forgot its closing bold') makes that item fail the item regex while still matching _SUB_OBJECTIVE_ITEM_LINE_RE. The cross-check at L498-505 correctly notices len(items)!=line_starts and emits _logger.warning — but then the function returns the SHORT list (items) anyway.
  - FIX: When line_starts != len(items), don't return the short list silently: raise/return empty so start_execution's no-sub-objectives branch opens a pause ticket, OR surface a structured 'parse_incomplete' signal the dispatcher turns into a pause-for-human ticket carrying the dropped line. The detection a
- **[MEDIUM/race]** `src/modulatio/project_execution.py:1034` (planning) — start_execution resumes from the STALE pre-lock plan snapshot, not the fresh reload taken under the lock — re-runs already-completed sub-objectives and double-counts budget under a daemon race
  - plan is loaded at L880 (before the lock). Under _claim_plan_lock the code reloads fresh (L974) for the status CAS, but then DISCARDS fresh: completed=plan.current_index (L1034), spawned_local/reflection_log_local from plan (L1035-36), and the BudgetTracker tokens_used/cost_usd_used/max_tokens/max_cost_usd all from the stale plan (L1051-55). plan_started_at also derives from plan.execution_started_
  - FIX: After the CAS passes, rebuild resume state from fresh: completed=fresh.current_index, spawned_local/reflection_log_local/tracker totals and plan_started_at from fresh (and re-extract sub_objectives from fresh.body if the body could change). Use fresh as the authoritative plan object for the loop.
- **[MEDIUM/error-path]** `src/modulatio/provider_catalog.py:405` (provider-catalog) — _is_free crashes the whole catalog parse when a model's `pricing` is a non-dict truthy value
  - In `_is_free`, `p = model.get("pricing") or {}` only replaces falsy values. If a provider feed returns `pricing` as a non-empty string/list (e.g. `"free"`), `p` stays that value and `p.get("prompt")` raises `AttributeError: 'str' object has no attribute 'get'`. This exception propagates out of `parse_models`, aborting the ENTIRE catalog (all 340+ OpenRouter models) instead of degrading. parse_mode
  - FIX: Guard the type: `p = model.get("pricing"); p = p if isinstance(p, dict) else {}`. Apply the same isinstance guard before `.get` in the extra-price-fields branch.
- **[MEDIUM/error-path]** `src/modulatio/provider_catalog.py:452` (provider-catalog) — parse_models aborts entire catalog on a non-string but truthy model `id` (AttributeError / ValidationError)
  - For a provider with `id_prefix_strip` set (Google), `mid = m.get("id")` can be a truthy non-string (e.g. int `123`); `mid.startswith(strip)` then raises `AttributeError`. For providers without strip (OpenRouter), the int id reaches `CatalogModel(id=mid,...)` and pydantic raises `ValidationError` (id must be str). Either way a single malformed entry kills the whole feed parse, contradicting the doc
  - FIX: In the loop, skip non-string ids: `if not isinstance(mid, str) or not mid: continue` (before the strip). Coerce name: `name = m.get("name"); name = name if isinstance(name, str) and name else mid`. This makes a single bad entry skip rather than abort the batch, matching the stated contract.
- **[MEDIUM/race]** `src/modulatio/runners.py:322` (runners-auth) — _rotated_pool_key has a second TOCTOU window that can return a PINNED base key
  - _rotated_pool_key guards on `provider_keys.pool_env_vars(pool_base)` being non-empty (line 322), then on line 324 calls `provider_keys.next_pool_env_var(pool_base)`, which RE-COMPUTES the pool independently. If the pool goes non-empty -> empty between those two calls (a pin added or the env var unset by a concurrent config edit), next_pool_env_var falls back to returning the bare `base_env_var` (p
  - FIX: Compute the pool ONCE inside _rotated_pool_key and index it directly (e.g. fetch pool_env_vars, if empty return None, else rotate a local cursor over that snapshot), OR make next_pool_env_var return None on an empty pool instead of falling back to base_env_var (its only production caller is _rotated
- **[MEDIUM/race]** `src/modulatio/heartbeat.py:319` (scheduling) — claim_next_pending's cross-process exclusivity is not delivered — _queue_lock is an in-process RLock, but the docstring claims it guards daemon-vs-CLI double dispatch
  - `claim_next_pending` holds `_queue_lock` (a module-level `threading.RLock`) across read-decide-write and its docstring states this 'closes the select-then-claim race' between 'a daemon heartbeat tick and a concurrent CLI heartbeat run-once against the same on-disk queue'. But an RLock only serializes threads WITHIN one process. The daemon and a separate `modulatio heartbeat run-once` CLI invocatio
  - FIX: Guard the select-and-claim with a cross-process file lock (fcntl.flock / lockf on the queue file or a sibling .lock) around load→select→save in claim_next_pending (and the cron load/save). Or make claim atomic via an O_EXCL claim-marker file per task id. At minimum, correct the docstrings to stop cl
- **[MEDIUM/error-path]** `src/modulatio/daemon.py:115` (scheduling) — Daemon log opened before CONFIG_DIR is created — fresh-install start crashes silently, parent reports false success
  - In the forked child, `_new_stdout = open(_log_file(), "a", buffering=1)` (line 115) runs BEFORE `config.CONFIG_DIR.mkdir(parents=True, exist_ok=True)` (line 148). `_log_file()` is `~/.config/modulatio/daemon.log`. On a fresh machine where `~/.config/modulatio/` does not yet exist (nothing in the daemon-start path, the CLI `daemon on` command, or `is_running()` creates it; config.py never mkdirs CO
  - FIX: Create `config.CONFIG_DIR.mkdir(parents=True, exist_ok=True)` at the very top of the child block (immediately after `os.setsid()`), before opening the log file at line 115. Alternatively call it in `start()` before the fork.
- **[MEDIUM/correctness]** `src/modulatio/setup_wizard/agent_step.py:234` (setup-wizard) — Wizard re-invocation silently drops previously-customized per-role context_budget
  - On re-invocation, _load_existing_state() pre-fills triad_agents/worker_agents from team_template.json, including any per-agent `context_budget` the user customized in a prior run. But _provision_triad() unconditionally rebuilds each role via `by_tier[tier] = _build_agent_from_template(template_id, model)` (line 234) and _provision_workers() rebuilds each producer via `_build_producer(model, index=
  - FIX: In _provision_triad and _provision_workers, carry forward `context_budget` (and any other non-rebuilt prior fields) from the pre-seeded by_tier/prior agent onto the freshly built dict, e.g. `if (prior_cb := by_tier.get(tier, {}).get('context_budget')) is not None: built['context_budget'] = prior_cb`
- **[MEDIUM/correctness]** `src/modulatio/setup_wizard/__init__.py:321` (setup-wizard) — Abort message claims removed models 'were saved and remain available' after a preset REMOVAL
  - The abort handler detects `presets_changed = _presets_snapshot() != presets_at_start` and, when true, emits 'Configured models were saved and remain available.' But provider_step._remove_flow() writes through remove_preset() immediately (model_presets.py:189), so deleting a pre-existing preset during a re-invocation and then aborting also makes presets_changed True. The message then asserts the co
  - FIX: Distinguish added vs removed by comparing key sets of the start/end snapshots. If keys were removed, say something like 'model changes (including removals) were written to disk'; only claim 'saved and remain available' when the delta is purely additive.
- **[MEDIUM/race]** `src/modulatio/inboxes.py:791` (skills-standards) — Audit-log JSONL append bypasses _APPEND_LOCK despite the documented invariant — torn rows under parallel waves
  - _emit_inbox_event writes to audit_path with its own unsynchronized `audit_path.open("a").write(json+"\n")` and takes NO _APPEND_LOCK. The module's lock contract (lines 53-66 and 391-392) explicitly claims _APPEND_LOCK 'serializes every shared-file JSONL append' to 'the shared tombstones / AUDIT / recipient files.' That claim is false for the standalone audit emissions: the `enqueue` audit row (lin
  - FIX: Wrap the audit write body of _emit_inbox_event (the mkdir/touch/open/write block at lines 785-792) in `with _APPEND_LOCK:` so it shares the same serialization as _append_jsonl_row. The lock is reentrant, so the decay path (which already holds it via _tombstone) is unaffected. Alternatively route the
- **[MEDIUM/integration]** `src/modulatio/backup.py:397` (store-types) — Backup round-trip breaks: export captures dotfile dirs (.obsidian/) that import unconditionally rejects, aborting the WHOLE restore
  - _walk_vault (export) only excludes parts in (".cache","_proposals","lance.db"), so it captures legitimate dotfile content inside a project — most notably .obsidian/* (the vault IS Obsidian per store.py's own docstring: 'Obsidian files are the primary source of truth'). On import, every captured rel_path is checked with tools._is_safe_relative_file_arg, which returns False for ANY component startin
  - FIX: Make the two halves agree. Export-side: in _walk_vault, skip any file whose relative path has a dot-prefixed component (mirror the .cache exclusion) so benign dotfiles never enter the archive, and count them in `skipped`. Import-side defense: when a rel_path fails the dotfile-but-not-traversal check
- **[MEDIUM/error-path]** `src/modulatio/store.py:184` (store-types) — Entity read uses locale-default encoding while write forces utf-8; under C-locale a valid non-ASCII entity is falsely quarantined as corrupt
  - _write_entity opens the temp file with encoding='utf-8' (line 225) but _read_entity calls path.read_text() with NO encoding (line 184), which uses locale.getpreferredencoding(). On a C/POSIX-locale deployment (a common cron/systemd environment with no LANG set), that is ASCII; reading a utf-8 entity file that contains any non-ASCII byte (an em-dash, an accented producer/agent name, a curly quote —
  - FIX: Read with the same explicit encoding the writer uses: path.read_text(encoding='utf-8') in store._read_entity, and encoding='utf-8' on the backup read_text() / json.loads(path.read_text(encoding='utf-8')) calls. (A binary/genuinely-non-utf-8 file still raises UnicodeDecodeError and correctly quaranti
- **[MEDIUM/error-path]** `src/modulatio/store.py:373` (store-types) — update_ticket_approval leaves ticket RESOLVED but plan unflipped (and raises) when plan flip fails with anything other than FileNotFoundError
  - In update_ticket_approval the ticket is written to disk as RESOLVED (line 355) BEFORE the linked-plan flip at lines 373-390. That block only swallows FileNotFoundError. If _plans.mark_approved/mark_declined raises anything else — a corrupt/un-parseable plan file (pydantic ValidationError = ValueError), or set_status raising ValueError on a disallowed transition — the exception propagates out of up
  - FIX: Either perform the plan flip BEFORE committing the ticket write (so a plan failure aborts the whole decision atomically), or broaden the except to log-and-continue on any plan-flip error the same way FileNotFoundError is tolerated, with a ticket transition note recording the un-reconciled plan so it
- **[MEDIUM/security]** `src/modulatio/tools.py:524` (tools-sandbox) — go vet -vettool=BIN / -flag=PATH bypass the passive allowlist (arbitrary-binary exec + path arg in a 'no-execution' profile)
  - _is_safe_go_target returns True for ANY arg starting with '-' (line 524: `if arg.startswith("-"): return True`). The #83 fix confined plain path args (traversal/absolute) but exempts every flag. `go vet -vettool=/path/to/binary ./...` makes go vet EXECUTE that binary — a direct violation of the passive profile's core contract ('nothing in argv causes user-controlled code to run'). Flag-with-value 
  - FIX: In _is_safe_go_target, do not blanket-accept flags. Reject -vettool entirely (it execs a binary) and, for any `-flag=value` shape, split on the first '=' and run the value through the same path-confinement check as a positional target (so `-tags=../../etc` is refused). Bare value-less flags like -js
- **[MEDIUM/security]** `src/modulatio/tools.py:710` (tools-sandbox) — Single-dash flags slip through _is_safe_file_arg as 'files' for ls/cat/head/tail/wc (passive allowlist tightness gap)
  - _is_safe_file_arg (and _is_safe_relative_file_arg) only reject empty / '-' / absolute / dotfile / traversal. A token like '-R', '--color=always', '-A' is NOT one of those, so it is accepted as a 'file' for every head that validates args purely via _is_safe_file_arg with no flag split: ls (line 714), cat (718), head/tail numeric-else branch, and wc's flag classifier permits behavior flags. So `ls -
  - FIX: Add an explicit per-head flag allowlist (mirroring the grep/wc flag split) so any token starting with '-' that is not in the head's permitted flag set is refused, rather than falling through to _is_safe_file_arg which treats it as a filename. At minimum reject leading-'-' tokens in ls/cat/head/tail 
- **[MEDIUM/integration]** `src/modulatio/tui/command_palette.py:77` (tui-core) — Command-palette tab switch is a silent no-op (query_one(TabbedContent) raises TooManyMatches)
  - ModulatioCommands._make_hit builds a _switch_to_tab callback that does self.app.query_one(TabbedContent) with no id. The composed TUI tree always contains at least three TabbedContent widgets simultaneously: #app-tabs (app.py:347), #console-streams (prompt.py:156, mounted on the initial-active tab-prompt), and #config-flip (app.py:352). Textual's App.query_one(Type) searches the whole DOM and rais
  - FIX: Scope the query to the top-level tab container: replace `tabs = self.app.query_one(TabbedContent)` with `tabs = self.app.query_one('#app-tabs', TabbedContent)` (mirroring app.py:822's _apply_side_effect switch_tab handler).
- **[MEDIUM/error-path]** `src/modulatio/tui/screens/models.py:63` (tui-screens-widgets) — Models tab DataTable crashes with MarkupError on bracketed agent name/model (status line hardened, rows were not)
  - `_refresh` adds raw `agent.id, agent.name, agent.model, agent.model_tier, agent.cost_class` to the DataTable. The R2 audit (tests/test_tui_screens_models_r2_audit.py) hardened only the *status Static lines* (now use rich escape) but did NOT cover the DataTable rows — they still pass raw strings through Text.from_markup. A user-named agent or a custom model id containing `[/...]` crashes the Models
  - FIX: Wrap each dynamic cell in `Text(...)` before add_row (shared `_cell()` helper), same as memory.py.
- **[MEDIUM/error-path]** `src/modulatio/tui/screens/agent_builder.py:95` (tui-screens-widgets) — Configuration/Agents roster DataTable crashes with MarkupError on bracketed agent name
  - `show_list` does `table.add_row(a.tier, a.name or a.id, model, status, key=a.id)` with raw `a.name`. Free-form agent names containing markup-closer sequences raise MarkupError when DataTable paints. Same unguarded class as agents.py/models.py.
  - FIX: Wrap dynamic cells (name/model) in `Text(...)` via a `_cell()` helper before add_row.
- **[MEDIUM/error-path]** `src/modulatio/tui/screens/jt_library.py:107` (tui-screens-widgets) — JT Library DataTable crashes with MarkupError on bracketed template description/name
  - `refresh_templates` does `table.add_row(e.name, e.description or '—', caps, key=e.name)` with raw strings. Job-template names/descriptions are authored (Leader create_job_template / Alfred loop / direct) and can contain brackets/code. A description containing a sequence like `out[/idx]` raises MarkupError at DataTable paint, crashing the JT Library tab. Unguarded vs the memory.py/tickets.py harden
  - FIX: Wrap e.name / e.description / caps in `Text(...)` before add_row.
- **[MEDIUM/error-path]** `src/modulatio/tui/screens/configuration.py:118` (tui-screens-widgets) — Configuration·Models list DataTable crashes with MarkupError on a custom model id containing brackets
  - `_fill_table` does `table.add_row(key, preset.get('model',''), preset.get('auth_type',''), 'ready ✓'/..., key=key)` with raw strings. The custom-provider flow lets the operator type an arbitrary model id (model_picker.py custom path -> register -> add_preset). A custom id or preset key containing a markup-closer (e.g. `my-model[/v2]`) raises MarkupError when the models list paints. Lower likelihoo
  - FIX: Wrap the model-id cell (and key) in `Text(...)` before add_row.
- **[MEDIUM/error-path]** `src/modulatio/tui/screens/skills.py:97` (tui-screens-widgets) — Skills tab DataTable crashes with MarkupError on bracketed skill description/name (paint-time, unrecoverable)
  - `_refresh` calls `table.add_row(s.name, s.description[:60], ', '.join(s.capability_tags), ...)` with RAW strings. Textual's DataTable `default_cell_formatter` (.venv/.../_data_table.py:220-221) runs every `str` cell through `Text.from_markup`, which raises `rich.errors.MarkupError` on sequences like `[/idx]`, `x[/2]`, `[/pattern/]`. Skill descriptions are LLM/user-authored capability documentation
  - FIX: Wrap every dynamic str cell in a `rich.text.Text` (verbatim, bypasses markup) exactly like memory.py's `_cell()`. Add a shared `_cell(value)` helper (or import memory.py's) and use it for s.name / s.description / capability tags / project-local in skills.py — and apply the same to the sibling DataTa
- **[MEDIUM/error-path]** `src/modulatio/tui/screens/agents.py:47` (tui-screens-widgets) — Agents tab DataTable crashes with MarkupError on a bracketed agent name/model
  - `_refresh` does `table.add_row(agent.id, agent.name, agent.tier, agent.model or '', ', '.join(agent.skills), key=...)` with raw strings. `agent.name` is free-form user input (roster.add_agent takes `name: str` with no markup validation) and `agent.model`/skills can contain brackets. DataTable's formatter runs Text.from_markup on each, so a name/model containing a sequence like `[/2]` or `v[/1]` ra
  - FIX: Wrap each dynamic cell in `Text(...)` (a `_cell()` helper) before add_row, matching memory.py.

## LOW
- **[correctness]** `src/modulatio/assembly.py:1033` (assembly-delivery) — CSV dedupe key can collide across differently-shaped rows  (fix: Use a structure-preserving serialization for the dedupe key (e.g. `json.dumps(r)` of the row list, or a length-prefixed/escaped join) so field boundaries are un)
- **[resource-leak]** `src/modulatio/assembly.py:992` (assembly-delivery) — `_CSV_FIELD_LIMIT_LOCK` serializes the entire CSV parse, not just the global save/restore window  (fix: Either accept the serialization explicitly (it is safe), or narrow the contention: parse with the raised limit inside the lock only long enough that the global )
- **[race]** `src/modulatio/cli_standards.py:36` (cli) — standards list double-scans the proposals dir (list_proposals + list_ids) — TOCTOU misalignment under concurrent staging  (fix: Add an `id`/`proposal_id` field to the Proposal dataclass (set from path.stem in _parse_file/list_proposals) and derive ids from the single list_proposals resul)
- **[error-path]** `src/modulatio/cli.py:1142` (cli) — cron list sort key not None-safe for an explicit `next_run: null` job  (fix: Use `(j.get('next_run') or '')` in the sort key, matching the display line.)
- **[resource-leak]** `src/modulatio/_crash.py:95` (cli) — Crash-log directory grows unbounded — no rotation/cap  (fix: After writing, prune crash-*.log beyond a cap (e.g. keep newest N or files newer than M days) in write_crash_log.)
- **[race]** `src/modulatio/compression.py:882` (context-budget) — compression audit appends to shared audit.jsonl with no write-lock, unlike context_budget which serializes via `write_lock`  (fix: Thread an optional `write_lock` (or the orchestrator `_store_lock`) through `emit_compaction` / `emit_compaction_*` and guard the append in `_append_audit_row_0)
- **[error-path]** `src/modulatio/comptroller.py:124` (cost-metering) — Non-UTF8 comptroller.md/ledger raises uncaught UnicodeDecodeError, defeating escalation degrade-open  (fix: Read with errors tolerated, e.g. path.read_text(encoding='utf-8', errors='replace'), or wrap load_budget/the scanners in a try that degrades to the safe default)
- **[cost]** `src/modulatio/runners.py:1014` (cost-metering) — Idempotent metered re-auth re-executes tool.call — provider re-charged though ledger isn't  (fix: When the first real adapter lands, either cache/short-circuit the idempotent result in the engine (don't re-call tool.call on a key_seen ALLOW), or require the )
- **[correctness]** `src/modulatio/comptroller.py:198` (cost-metering) — Corrupt/truncated metered lines counted inconsistently between the two metered scanners  (fix: Make both scanners require the full 6-field metered shape (len(parts) >= 6) before counting, so a malformed line is ignored uniformly by both.)
- **[correctness]** `src/modulatio/config.py:202` (cross-cutting) — set_env_secret silently drops comments/blank lines from the vault .env on rewrite  (fix: Preserve comment/blank lines on rewrite (parse to an ordered structure that keeps non-kv lines), matching remove_env_secret's preserve-and-rewrite approach.)
- **[correctness]** `src/modulatio/config.py:210` (cross-cutting) — Newline (or '=') in an API key value injects/corrupts vault .env lines  (fix: Reject or sanitize values containing newlines in set_env_secret (or in provider_keys.add_key), or quote/escape on write and parse accordingly.)
- **[race]** `src/modulatio/team_state.py:400` (cross-cutting) — Shared fixed-name .tmp for state-doc writes can collide if two writers race  (fix: Use a unique temp name (tempfile.mkstemp in path.parent) like config.write_secret_file, or document/guard that state-doc writes are single-writer-per-run.)
- **[edge-case]** `src/modulatio/export.py:106` (cross-cutting) — export_artifact markdown no-op copy raises SameFileError when source==dest  (fix: Short-circuit when source.resolve()==dest.resolve() (return success), or catch SameFileError and return an ExportResult/ExportError.)
- **[product-agnostic]** `src/modulatio/orchestration.py:526` (dispatch-recovery) — #73 family default-to-document rewrites a directly-tool-produced render binary's evidence to .md (divergence for non-assembled .pptx/.pdf/.docx)  (fix: Only apply render-path normalization when the family is document AND the task is actually on a document-AUTHOR-then-render route. Either: (1) gate _normalize_re)
- **[error-path]** `src/modulatio/recoveries.py:354` (dispatch-recovery) — load_recoveries / consumed_ids leak UnicodeDecodeError on a non-UTF-8 log file, defeating the module's fail-open contract  (fix: Read defensively: use p.read_text(errors='replace') (or catch (OSError, UnicodeDecodeError) / catch ValueError around the read) in both load_recoveries and cons)
- **[correctness]** `src/modulatio/telegram_listener.py:263` (interfaces) — Quoted command token corrupts args_text slice  (fix: Derive args from the shlex parts (shlex.join(parts[1:]) or re-split args from parts), or strip the original token by matching the un-split prefix, rather than s)
- **[resource-leak]** `src/modulatio/acp/server.py:235` (interfaces) — ACP sessions never reaped — _sessions and _session_pending grow unbounded  (fix: Add a session/close handler (or reap on cancel when the client signals teardown) that drops the entry from _sessions and _session_pending and releases the orche)
- **[product-agnostic]** `src/modulatio/chat.py:180` (interfaces) — Document attachment content fenced with bare ``` can be broken by content containing a fence  (fix: Use a dynamically-sized fence (count the longest backtick run in content and use one longer), or a unique delimiter, when wrapping attachment content.)
- **[resource-leak]** `src/modulatio/memory/agent_memory.py:221` (memory) — add_semantic prune overshoots SEMANTIC_MAX_ENTRIES when active entries exceed the cap  (fix: Cap the combined result: entries = (inactive[-(MAX//5):] + active)[-MAX:], or compute the active budget as MAX - kept_inactive.)
- **[correctness]** `src/modulatio/qc_history.py:204` (memory) — Verdict filename ordering breaks within the same second (timespec='seconds')  (fix: Use a higher-resolution monotonic prefix (mirror team_memory._new_id with %f + counter) or a per-domain append counter so filenames sort by true append order.)
- **[error-path]** `src/modulatio/orchestration.py:5379` (orch-converse) — converse(): final leader-turn append is outside the try, can leave the durable log ending on an unanswered operator turn  (fix: Move the success-path `_append_conversation("leader", reply)` inside the same try, or wrap it so any append failure still records a placeholder leader turn (bes)
- **[error-path]** `src/modulatio/orchestration.py:11219` (orch-converse) — _pin_attachments write_text catches (OSError, TypeError) but not UnicodeEncodeError (a ValueError), breaking the 'one bad attachment doesn't abort pinning' contract  (fix: Add ValueError (or UnicodeError) to the write-side except: `except (OSError, TypeError, ValueError): continue` to match the read-side and honor the per-attachme)
- **[resource-leak]** `src/modulatio/orchestration.py:4700` (orch-converse) — Conversation log + per-turn full re-read grow unbounded across a long-lived project  (fix: Tail-read the file (or keep a bounded read window / periodic compaction) so _load_conversation only materializes the most recent ~window turns, and/or rotate th)
- **[error-path]** `src/modulatio/orchestration.py:4224` (orch-exec) — `_producer_patch` reads the iteration-target file with bare `read_text()` — UnicodeDecodeError would mask as a confusing BLOCKED  (fix: Wrap the `current = path.read_text()` in `_producer_patch` in `try/except (OSError, UnicodeDecodeError)` and, on failure, fall through to a clean generate (or r)
- **[resource-leak]** `src/modulatio/orchestration.py:6791` (orch-exec) — `_seed_staging` full copytree of the growing shared artifacts tree per task per wave is O(tasks x tree-size)  (fix: Seed staging more cheaply for the common case — e.g. hardlink-clone the seed (copytree with a hardlink copy_function where the FS supports it, or only seed the )
- **[error-path]** `src/modulatio/orchestration.py:8592` (orch-goal) — Verify JSON parse-failure leaves the goal stranded IN_PROGRESS on a normal finish  (fix: On the parse-failure path, drive the goal to a terminal state with a PQR reservation (reuse _settle_zero_completed-style settle, or a dedicated 'verify-unparsea)
- **[integration]** `src/modulatio/orchestration.py:10755` (orch-goal) — Reopened goals containing a cross-goal task dependency silently lose dep-ordering (topo-sort raises, falls back to store order)  (fix: Load the full project task set (or at least the transitive cross-goal deps) into task_map for ordering/gating so _topological_sort doesn't trip on cross-goal id)
- **[correctness]** `src/modulatio/orchestration.py:8396` (orch-goal) — Dead method _refresh_daily_budget_if_new_day — defined, documented as 'called before any auto-redo', never called  (fix: Delete the method (the inline reset in _auto_resume_refreshable_goals is the live path) or, if kept for tests, rewrite the docstring to state it is NOT used in )
- **[resource-leak]** `src/modulatio/orchestration.py:6643` (orch-wave-assembly) — Crashed wave worker silently drops its deferred store-writes and decompose children  (fix: Capture the worker's already-accumulated deferred/child buffers into the synthetic result on the crash path (the worker still has the lists in scope; pass them )
- **[resource-leak]** `src/modulatio/orchestration.py:3403` (orch-wave-assembly) — _effective_assembly_family re-reads + reparses standards from disk per-evidence-item with no caching  (fix: Compute the family ONCE per spec (before the sub-task/evidence loops) and reuse it; or add functools.lru_cache to standards.load_with_metadata keyed on (domain,)
- **[correctness]** `src/modulatio/plans.py:457` (planning) — Nested **bold** inside a sub-objective title corrupts parsed title/description (lazy match stops at first **) without tripping the cross-check  (fix: Anchor the title close to the LAST ** before end-of-line (greedy with a trailing-separator lookahead) or strip inner ** from the captured title; low priority si)
- **[resource-leak]** `src/modulatio/project_execution.py:68` (planning) — _claim_plan_lock creates <plan_id>.lock files in the plans dir that are never removed — unbounded (one per plan id) accumulation  (fix: Acceptable to leave (lock files are tiny and bounded by plan count); if cleanup desired, unlink on terminal plan status. Document the intent.)
- **[correctness]** `src/modulatio/project_execution.py:1934` (planning) — tick() slices discovered[:max_per_tick] BEFORE the staleness skip — a stale lowest-id plan starves the tick of any progress that iteration  (fix: Slice AFTER filtering: iterate discovered, skip stale, and take up to max_per_tick that are still approved.)
- **[error-path]** `src/modulatio/project_execution.py:1960` (planning) — tick() reports final_status='executing' for project_loader/runners_for failures even though the plan never started and is still 'approved' on disk  (fix: Report final_status from the freshest on-disk status (fresh.status, already loaded at L1943) instead of the hardcoded 'executing' for the loader/runners failure)
- **[correctness]** `src/modulatio/model_capabilities.py:71` (provider-catalog) — Bare `o1`/`o3`/`o4` substrings in _FAMILY_TABLE mis-infer unrelated models as OpenAI reasoning/premium tier  (fix: Anchor these to a likelier token boundary (e.g. require a leading slash/dash or the family prefix like `gpt-o1`/`openai/o1`, or match `o1-`,`o3-`,`o4-`/`o1`/`o3)
- **[error-path]** `src/modulatio/provider_catalog.py:498` (provider-catalog) — _load_picklist raises (not degrades) if the packaged seed file is missing or corrupt  (fix: Wrap the read/parse in try/except returning `[]` (mirroring local_probe's graceful-empty pattern), or validate the parsed value is a dict before `.get`.)
- **[integration]** `src/modulatio/orchestration.py:4673` (runners-auth) — run_llm_with_tools is never wired with metered_authorizer on the production path -> all metered cloud tools always DENIED  (fix: Thread a metered_authorizer (comptroller.authorize_metered_tool bound to task/artifact context) from _run_chat_loop / _llm_with_tools_execute into run_llm_with_)
- **[error-path]** `src/modulatio/runners.py:481` (runners-auth) — Responses-API path in litellm_runner skips OAuth refresh AND 429 pool failover  (fix: Factor the auth-refresh-once and pool-failover handling into a shared helper and apply it to both the responses() and completion() call sites, or at least add _)
- **[race]** `src/modulatio/provider_keys.py:96` (runners-auth) — list_keys iterates os.environ.keys() while config edits mutate os.environ -> possible 'dict changed size during iteration'  (fix: Snapshot the environment once at the top of list_keys: `env_names = list(os.environ.keys())` (and read values from that snapshot or via os.environ.get afterward)
- **[cost]** `src/modulatio/runners.py:542` (runners-auth) — AuthenticationError refresh path for a pooled api_key preset can retry with the (possibly pinned) base key  (fix: On the api_key/pooled retry path, re-draw from _pooled_call_key(pool_base, model) instead of the strategy's base-var token, or skip the refresh-retry entirely f)
- **[correctness]** `src/modulatio/runners.py:262` (runners-auth) — default_params can override authoritative api_key for a none-auth preset  (fix: After resolving the strategy, explicitly pop/forbid api_key and api_base from the default_params-derived kwargs unless they came from the dedicated base_url/tok)
- **[correctness]** `src/modulatio/heartbeat.py:570` (scheduling) — Completed task overwrites a concurrent cancel — done/failed write clobbers a status=cancelled set mid-dispatch  (fix: In `_run_task`'s completion update, re-read the task under the lock and skip the status write if it was moved to a terminal state (cancelled) out-of-band, or ha)
- **[correctness]** `src/modulatio/heartbeat.py:284` (scheduling) — Unparseable next_run is treated as eligible-now, silently bypassing a recurring task's schedule  (fix: Decide intent explicitly: either skip the task (treat unparseable next_run as not-yet-eligible) or mark it failed/stale, rather than silently running it now.)
- **[resource-leak]** `src/modulatio/daemon.py:214` (scheduling) — Module-global _shutdown Event is never cleared at _run_daemon entry  (fix: Call `_shutdown.clear()` at the top of `_run_daemon` (and reset signal handlers idempotently).)
- **[correctness]** `src/modulatio/setup_wizard/agent_step.py:431` (setup-wizard) — Team-floor guard `total < MIN_AGENTS` is unreachable dead code  (fix: Either remove the dead branch, or make it a real engine invariant by asserting the structural-pair cardinality (len(triad)==2) rather than a sum that the prior )
- **[correctness]** `src/modulatio/setup_wizard/agent_step.py:213` (setup-wizard) — agent_step hardcodes step_header(4, 7, ...) while the wizard runs 9 steps  (fix: Pass the real (index, total) through, or derive the agents step's position from the step order constant rather than hardcoding 4/7.)
- **[product-agnostic]** `src/modulatio/setup_wizard/agent_step.py:313` (setup-wizard) — Context-budget UX hardcodes 'drafting'/'verification' role prose but budgets are model-agnostic-by-role — acceptable, with one producer-agnostic smell  (fix: Reword the producer note to an artifact-neutral term ('production'/'generation'). No logic change.)
- **[resource-leak]** `src/modulatio/inboxes.py:83` (skills-standards) — _WARNED_SOFT_CAP grows unbounded across runs in a long-lived daemon (never cleared)  (fix: Either key the dedup on run_id and drop the per-run subset when a run finalizes (e.g. an inboxes.clear_run_warnings(run_id) called at run teardown), or bound th)
- **[product-agnostic]** `src/modulatio/standards_proposals.py:122` (skills-standards) — standards_proposals writes proposal/standards files without explicit utf-8 while standards.py reads them as explicit utf-8 — locale-dependent corruption asymmetry  (fix: Add encoding="utf-8" to every write_text/read_text/open in standards_proposals.py (and ideally lessons.py) so the write side matches standards.py's explicit-utf)
- **[race]** `src/modulatio/inboxes.py:1366` (skills-standards) — Candidate abandonment can double-emit terminal+audit rows when list_pending_candidates and sweep_abandoned_candidates run for the same turn/candidate  (fix: Make the read-check-write of the terminal transition atomic under _APPEND_LOCK (re-read terminal ids inside the lock immediately before _record_candidate_termin)
- **[correctness]** `src/modulatio/store.py:245` (store-types) — Quarantining the highest-numbered ticket lets _next_ticket_number reuse that ticket ID  (fix: In _next_ticket_number, also consider quarantined siblings: strip a trailing '.broken[.<ts>]' before the isdigit() check, or scan '{prefix}*' (including .broken)
- **[race]** `src/modulatio/store.py:110` (store-types) — Quarantine collision name uses second-resolution timestamp; two same-second quarantines silently overwrite  (fix: Use a collision-proof suffix (e.g. a short token_hex or a tempfile.mkstemp-style unique name) instead of the second-resolution timestamp, and avoid renaming ont)
- **[resource-leak]** `src/modulatio/tools.py:1105` (tools-sandbox) — Popen pipes can leak fds on the pathological triple-timeout drain path  (fix: Wrap the Popen body in try/finally (or use `with subprocess.Popen(...) as proc:`) and explicitly close proc.stdout/proc.stderr if still open after the final dra)
- **[race]** `src/modulatio/tui/app.py:1051` (tui-core) — F8 STOP only signals the first active orchestrator when both converse-job and direct kickoff run concurrently  (fix: Make _running_job_orchestrator / action_stop_job signal abort_event on ALL active orchestrators (loop and .set() each), not just return the first; and gate _kic)
- **[race]** `src/modulatio/tui/app.py:791` (tui-core) — converse-done force-settle can falsely settle TEAM spinner during a kickoff's startup window  (fix: Set self._kickoff_orch (or a 'kickoff pending' flag) on the main thread in _run_kickoff before scheduling the worker, so the guard sees the in-flight kickoff im)
- **[error-path]** `src/modulatio/tui/screens/prompt.py:234` (tui-screens-widgets) — Attach-failed status uses unescaped exception text in a markup-enabled Static  (fix: Escape the dynamic part: `from rich.markup import escape; ...update(f"[bold red]Attach failed:[/] {escape(str(exc))}")` in both attach_kickoff and attach_chat.)